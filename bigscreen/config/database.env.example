# 复制本文件为 database.env，再按本机数据库修改。
# Linux 一键部署脚本会在文件不存在时自动复制本模板，并在需要时创建应用账号。
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=charging_screen

# mysql：读取MySQL结果表；csv：直接读取output/ads中的Spark结果。
DATA_SOURCE=mysql
CACHE_TTL=30
