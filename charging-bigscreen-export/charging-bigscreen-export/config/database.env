# 数据库连接配置：Flask 后端与 Spark 装载脚本共用
# 环境变量优先级高于本文件，便于在虚拟机/服务器上覆盖
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=charging_screen

# 后端数据源：mysql（默认，读取 Spark 结果表）或 csv（无 MySQL 时兜底）
DATA_SOURCE=mysql
# 大屏数据缓存时间（秒）
CACHE_TTL=30
